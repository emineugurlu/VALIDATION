name: Lint HTML

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: HTML Hint
        uses: htmlhint/htmlhint-action@v2
        with:
          pattern: '**/*.html'
